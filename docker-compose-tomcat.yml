version: '3.7'

services:
  tomcat1:
    build: tomcat
  wrk1:
    build: wrk-tomcat
    sysctls:
      - net.ipv4.ip_local_port_range=1024 65000
    links:
      - tomcat1 
  wrk2:
    build: wrk-tomcat
    sysctls:
      - net.ipv4.ip_local_port_range=1024 65000
    links:
      - tomcat1
  wrk3:
    build: wrk-tomcat
    sysctls:
      - net.ipv4.ip_local_port_range=1024 65000
    links:
      - tomcat1
